## Packages
recharts | Data visualization for skill importance and scores
framer-motion | Smooth page transitions and entry animations
react-dropzone | Drag and drop file upload handling
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["'Outfit'", "sans-serif"],
  body: ["'Inter'", "sans-serif"],
}
API Endpoint /api/analyze/resume expects multipart/form-data
API Endpoint /api/analyze/job expects application/json
